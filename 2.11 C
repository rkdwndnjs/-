/*#include <stdio.h>
#incluwde <stdlib.h>
#include <time.h>
int main()
{	
	int result,question,try;
	
	try = 0;
	srand((unsigned int)time(NULL));
	question = rand() % 100 + 1;
	while(1)
	{
		try ++;
		printf("정답을 추측하여 보시오 ");
		scanf("%d", &result);
		if (result == question)
		{
			printf("축하합니다\n 시도횟수=%d",try);
			break;
		}
		

		else if (result >= question)
			printf("제시한 점수가 높습니다\n");
		else
			printf("제시한 점수가 낮습니다\n");
	}
}*/
/*#include <stdio.h>
int main()
{
	int i, sum;

	sum = 0;
	for (i = 1 ;i <= 10 ; i++)
		sum += i;

	printf("1부터 10까지 정수의 합 = %d\n", sum);
	return 0;
}*/

/*#include <stdio.h>
int main()
{
	int a, b, c, number;
	int count = 0;
	for (a = 1; a < 100; a++) {
		for (b = 1; b < 100; b++) {
			for (c = 1; c < 100; c++) {
				if (b>a && a * a + b * b == c * c) {
					printf("%d %d %d\n", a, b, c);
					count++;
				}
			}
		}
	}
	printf("직각 삼각형의 개수는 %d\n", count);
}*/

/*#include <stdio.h>
int main()
{
	int s1, s2, s3 ;

	for (s1 = 1; s1 <= 6; s1++) {
		for (s2 = 1; s2 <= 6; s2++) {
			if (s1 <= s2 && s1 + s2 == 6)
			{
				printf("두 주사위의 합이 6이 되는 경우 ");
				printf("%d %d \n", s1, s2);
			}
		}
	}
	for (s1 = 1; s1 <= 6; s1++) {
		for (s2 = 1; s2 <= 6; s2++) {
			for (s3 = 1; s3 <= 6; s3++) {
				if (s1 <= s2 && s2 <= s3 && s1 + s2 + s3 == 10)
				{
					printf("세 주사위의 합이 10이 되는 경우 ");
					printf("%d %d %d\n", s1, s2, s3);

				}
			}
		}
	}
}*/ 
/*파라미터 목록의 void와 return은 생략 가능
  하지만 리스트값의 void는 생략하면 안된다(생략시 int로 전환)*/
